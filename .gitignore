# Model weights (never commit these)
models/**/*.bin
models/**/*.safetensors
models/**/*.gguf
*.pt

# Benchmark output (raw data, regenerable)
results/*
!results/.gitkeep

# Downloaded benchmark datasets (large, regenerable via scripts/download_dataset.sh)
datasets/

# Python
__pycache__/
*.pyc
.venv/
venv/
.venv-reports/

# Env / secrets
.env
*.pem

# OS cruft
.DS_Store

# Internal dev notes / instructions - not needed in the public repo
CLAUDE.md
dev/active/
